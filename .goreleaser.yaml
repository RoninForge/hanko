version: 2

project_name: hanko

before:
  hooks:
    - go mod tidy
    - go mod verify

builds:
  - id: hanko
    main: ./cmd/hanko
    binary: hanko
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X github.com/RoninForge/hanko/internal/version.version={{.Version}}
      - -X github.com/RoninForge/hanko/internal/version.commit={{.FullCommit}}
      - -X github.com/RoninForge/hanko/internal/version.buildDate={{.Date}}

archives:
  - id: default
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    # Windows users expect .zip. macOS and Linux keep .tar.gz (goreleaser
    # v2 default), which matches the install.sh downloader.
    format_overrides:
      - goos: windows
        format: zip
    files:
      - LICENSE
      - README.md
      - CHANGELOG.md

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

sboms:
  - artifacts: archive

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  use: github
  sort: asc
  groups:
    - title: Features
      regexp: "^.*feat[(\\w)]*:+.*$"
      order: 0
    - title: Bug fixes
      regexp: "^.*fix[(\\w)]*:+.*$"
      order: 1
    - title: Docs
      regexp: "^.*docs[(\\w)]*:+.*$"
      order: 2
    - title: Other
      order: 999
  filters:
    exclude:
      - "^test:"
      - "^chore:"
      - "^ci:"
      - "Merge pull request"
      - "Merge branch"

release:
  github:
    owner: RoninForge
    name: hanko
  draft: false
  prerelease: auto
  header: |
    ## hanko {{ .Tag }}

    Validate Claude Code plugin manifests before you submit them. Go binary,
    zero network calls, pretty errors.

    **Install:**

    ```sh
    curl -fsSL https://roninforge.org/hanko/install.sh | sh
    ```

  footer: |
    ---

    **Full changelog**: https://github.com/RoninForge/hanko/compare/{{ .PreviousTag }}...{{ .Tag }}
